---
to: src/components/<%= props.pascalCaseName %>/<%= props.pascalCaseName %>.test.tsx
---
import React from 'react';
import { render, screen } from '@testing-library/react';

import <%= props.pascalCaseName %> from './index';

describe('<%= props.pascalCaseName %> Component', () => {
    it('should render <%= props.pascalCaseName %> Component', () => {
        // Arrange
        render(<<%= props.pascalCaseName %> />);

        // Act
        const element = screen.getByText('<%= props.pascalCaseName %> component!', { exact: false });

        // Assert
        expect(element).toBeInTheDocument();
    });
});
